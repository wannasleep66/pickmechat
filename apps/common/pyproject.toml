[project]
name = "common"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "faststream>=0.6.6",
    "pydantic>=2.12.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["common"]
